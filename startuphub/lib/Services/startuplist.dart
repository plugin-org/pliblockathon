/*import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import 'package:startup_hub/Services/form.dart';
import 'package:startup_hub/Widgets/patentContainer.dart';

class startuplist extends StatelessWidget {
  const startuplist({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      itemBuilder: (context, index) {
        final fo = formd().startup[index];
        return patentContainer(
            teamName: fo.startupname, description: fo.description);
      },
      itemCount: formd().startupCount,
    );
  }
}*/
